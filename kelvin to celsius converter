#include <stdio.h>

int main() {
    float temperature;                                 
    printf("enter temperature in kelvin: ");
    scanf("%f", &temperature);

    float celsius = temperature - 273.15;                      // conversion formula: celsius = kelvin - 273.15

    printf("temperature in degree celsius is %f \n", celsius);
    return 0;
}
