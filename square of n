// a program to print the square of a number given by the user 

#include<stdio.h>
#include<math.h>

int main (){
    int n; 
    printf("enter a number: ");
    scanf("%d", &n);
    printf("square of %d is %f", n, pow(n,2));

    return 0;
}

    
